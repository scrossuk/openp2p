#ifndef OPENP2P_JOIN_HPP
#define OPENP2P_JOIN_HPP

#include <boost/utility.hpp>
#include <OpenP2P/WaitHandler.hpp>

namespace OpenP2P{

	/*class Join: boost::noncopyable{
		public:
			Join(WaitHandler, WaitHandler);

			CancelFunction operator()(WaitCallback);

		private:
			Signal signal_;
			Signal completeSignal_;

	};*/

}

#endif
